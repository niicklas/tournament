@model IEnumerable<Tournaments.Models.Tournament>

@{
    ViewBag.Title = "Index";
    ViewBag.Description = "Tournament helper helps you crate and manage a tournamnet" ;
    ViewBag.Keywords = "Tournament";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.Rounds)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.TournamentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.Rounds)
        </td>*@
        <td>
            @*@Html.DisplayFor(modelItem => item.TournamentName)*@
            @Html.ActionLink(item.TournamentName, "Details", new { id = item.Id })
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
